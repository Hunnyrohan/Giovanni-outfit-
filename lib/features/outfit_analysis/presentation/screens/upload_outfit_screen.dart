import 'package:flutter/material.dart';

class UploadOutfitScreen extends StatelessWidget {
  const UploadOutfitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Analyze Outfit'),
      ),
      body: const Center(
        child: Text('Upload Outfit Screen Placeholder'),
      ),
    );
  }
}
